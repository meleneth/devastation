map $http_upgrade $connection_upgrade {
  default upgrade;
  '' close;
}

server {
  listen 443 ssl;
  server_name iam.{{ devastation_domain }};
  ssl_certificate /etc/nginx/certs/iam.{{ devastation_domain }}/tls.crt;
  ssl_certificate_key /etc/nginx/certs/iam.{{ devastation_domain }}/tls.key;

  location / {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    client_max_body_size 0;
    proxy_pass http://iam:8080;
  }
}

server {
  listen 443 ssl;
  server_name mail.{{ devastation_domain }};
  ssl_certificate /etc/nginx/certs/mail.{{ devastation_domain }}/tls.crt;
  ssl_certificate_key /etc/nginx/certs/mail.{{ devastation_domain }}/tls.key;

  location / {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    client_max_body_size 0;
    proxy_pass http://mail:{{ mailpit_ui_port }};
  }
}

server {
  listen 443 ssl;
  server_name keystone.{{ devastation_domain }};
  ssl_certificate /etc/nginx/certs/keystone.{{ devastation_domain }}/tls.crt;
  ssl_certificate_key /etc/nginx/certs/keystone.{{ devastation_domain }}/tls.key;

  location / {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    client_max_body_size 0;
    proxy_pass http://keystone:3000;
  }
}

server {
  listen 443 ssl;
  server_name s3.{{ devastation_domain }};
  ssl_certificate /etc/nginx/certs/s3.{{ devastation_domain }}/tls.crt;
  ssl_certificate_key /etc/nginx/certs/s3.{{ devastation_domain }}/tls.key;

  location / {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    client_max_body_size 0;
    proxy_pass http://seaweed:8333;
  }
}

server {
  listen 443 ssl;
  server_name seaweed.{{ devastation_domain }};
  ssl_certificate /etc/nginx/certs/seaweed.{{ devastation_domain }}/tls.crt;
  ssl_certificate_key /etc/nginx/certs/seaweed.{{ devastation_domain }}/tls.key;

  location / {
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto https;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    client_max_body_size 0;
    proxy_pass http://seaweed:8888;
  }
}
